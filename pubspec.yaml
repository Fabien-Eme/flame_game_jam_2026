name: flame_game_jam_2026
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  flame: ^1.35.1
  gamepads: ^0.1.9
  flutter_soloud: ^3.5.0
  shared_preferences: ^2.5.4
  http: ^1.6.0 


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  build_runner: ^2.12.2
  flutter_gen_runner: ^5.13.0

flutter:
  uses-material-design: true

  shaders:
    - shaders/crt.frag
    
  assets:
    - assets/

